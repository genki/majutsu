# majutsu v0.3.0 release evidence

## release

- tag: v0.3.0
- version: 0.3.0
- build: 1
- version output: `mj 0.3.0+build.1`
- date UTC: 2026-06-10
- host: vagrant
- rustc: 1.96.0
- cargo: 1.96.0

## local verification

```sh
cargo fmt --all
cargo check --locked
cargo test --locked
scripts/check-completion.sh
```

Result: pass

## release artifact

```sh
scripts/package-release.sh
tar -tf dist/majutsu-0.3.0-build.1-linux-x86_64.tar.gz
dist/majutsu-0.3.0-build.1-linux-x86_64/mj --version
dist/majutsu-0.3.0-build.1-linux-x86_64/mj --help
```

- artifact: `dist/majutsu-0.3.0-build.1-linux-x86_64.tar.gz`
- artifact size: 6.0 MiB
- `mj --version`: `mj 0.3.0+build.1`
- `mj --help` check: pass

## provider validation

| Provider | Status | Evidence |
|---|---|---|
| File remote | local gate verified | `cargo test --locked` |
| GCS S3-compatible endpoint | verified 2026-06-10 | 80 small-file root add sync: 98 objects / 3.824s; single-file edit sync: 14 objects / 2.332s |
| MinIO via Podman | not rerun for v0.3.0 | v0.2.0 local gate covered MinIO; v0.3.0 changed S3 sync latency path and was verified against GCS |
| AWS S3 | experimental | archive restore drill not run for this release |
| Cloudflare R2 | experimental | manual validation not run for this release |

## highlights

- S3/GCS sync latency improvement for small object batches.
- Sync cache avoids repeated HEAD requests for already-synced content-addressed objects.
- S3 remote uses canonical object aliases for tree/blob/pack/large manifest payloads while file remote keeps legacy compatibility.
- `mj --version` now includes a build number as SemVer build metadata.

## build number policy

`BUILD_NUMBER` is incremented for coherent implementation or verification batches within the same semantic version line. For this release it starts at `1`.
