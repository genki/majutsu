# majutsu v0.6.10 release evidence

## release

- version: 0.6.10
- date: 2026-07-06 JST
- version output: `mj 0.6.10`

## summary

- Patch release from the current main branch after validating root-owned
  fanotify daemon operation in the vagrant environment.
- Verified the generated systemd service shape includes `--backend fanotify`,
  `User=root`, `MemoryMax=2048M`, and `OOMPolicy=stop`.
- Confirmed the protected runtime state after fanotify daemon deployment:
  local and remote heads synced, upload queue empty, event journal empty, and
  no health issues.
- Updated README install example to point at the latest published patch
  version.

## validation

```sh
cargo fmt --all
cargo check --locked -q
cargo clippy --workspace --all-targets --locked -q -- -D warnings
cargo test --locked -q --bin mj
scripts/sync-internal-crates.sh --check
scripts/package-release.sh
scripts/publish-crates-io.sh
```

- result: pass
- coverage: fmt, check, clippy, binary unit tests, publish-facing internal
  mirror check, release package smoke, and crates.io dry-run.
- note: Podman MinIO was not rerun for this patch release because the release
  changes are versioning/documentation plus deployment validation of an already
  generated service shape.

## release artifact

```sh
scripts/package-release.sh
target/release/mj --version
tar -tf target/dist/majutsu-0.6.10-linux-x86_64.tar.gz
tmp=$(mktemp -d)
tar -xzf target/dist/majutsu-0.6.10-linux-x86_64.tar.gz -C "$tmp"
"$tmp"/majutsu-0.6.10-linux-x86_64/mj --version
"$tmp"/majutsu-0.6.10-linux-x86_64/mj --help
```

## crates.io

Only the root `majutsu` crate is published.

Publish:

```sh
CARGO_REGISTRY_TOKEN=... scripts/publish-crates-io.sh --execute
```

Install verification:

```sh
cargo install majutsu --version 0.6.10 --locked --root "$(mktemp -d)"
cargo search majutsu --limit 1
```

## GitHub Release

```sh
gh release create v0.6.10 \
  target/dist/majutsu-0.6.10-linux-x86_64.tar.gz \
  target/dist/majutsu-0.6.10-linux-x86_64.zip \
  --title 'majutsu v0.6.10' \
  --notes-file docs/releases/v0.6.10.md
```
