# majutsu v0.2.0 release evidence

## release

- tag: v0.2.0
- commit: release tag commit
- date UTC: 2026-06-09
- host: vagrant
- rustc: 1.96.0
- cargo: 1.96.0

## local completion gate

```sh
MAJUTSU_RUN_MINIO_E2E=1 scripts/check-completion.sh
```

Result: pass

Included checks:

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked`
- `cargo test --workspace --all-targets --locked`
- local file remote E2E
- encrypted disaster recovery E2E
- large object roundtrip E2E
- prune / gc safety E2E
- daemon status / metrics smoke E2E
- release package smoke
- Podman MinIO S3-compatible E2E

MinIO E2E result:

```text
remote s3://majutsu/e2e
objects 40
metadata ok
range_get 1
remote fsck quick ok
MinIO E2E が通過しました
majutsu completion check が通過しました
```

## local release artifact

```sh
scripts/package-release.sh
tar -xzf dist/majutsu-0.2.0-linux-x86_64.tar.gz -C "$tmp"
"$tmp"/majutsu-0.2.0-linux-x86_64/mj --version
"$tmp"/majutsu-0.2.0-linux-x86_64/mj --help
```

- artifact: `dist/majutsu-0.2.0-linux-x86_64.tar.gz`
- artifact size: 5.9 MiB
- `mj --version`: `mj 0.2.0`
- `mj --help` check: pass

## provider validation

| Provider | Status | Evidence |
|---|---|---|
| File remote | local gate verified | `tests/e2e_local.rs`, `tests/remote_file.rs` |
| MinIO via Podman | local gate verified | `MAJUTSU_RUN_MINIO_E2E=1 scripts/check-completion.sh` |
| GCS S3-compatible endpoint | verified 2026-06-09 | `~/moon` root sync / remote check / remote fsck / clone / restore |
| AWS S3 | experimental | archive restore drill not run for this release |
| Cloudflare R2 | experimental | manual validation not run for this release |

## archive restore

- provider: not validated for v0.2.0
- status: experimental

## encryption

- implementation: covered by encrypted disaster recovery E2E
- workspace `~/.majutsu`: encrypted S3-compatible backend configured
- master key export drill: covered by local E2E
